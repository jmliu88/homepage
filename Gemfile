source "https://rubygems.org"

# Jekyll 4.x — compatible with both macOS system Ruby and newer Homebrew Ruby.
# If you are on macOS system Ruby 2.6, also add:  gem "ffi", "< 1.17"
gem "jekyll", "~> 4.2"

# Not bundled with Ruby >= 3.0 (needed by `jekyll serve`)
gem "webrick"

group :jekyll_plugins do
  # No plugins required — the site builds cleanly on GitHub Pages too.
end
